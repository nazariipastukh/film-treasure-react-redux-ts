import {createSlice} from "@reduxjs/toolkit";

const initialState = {
    cast: [] = []
}

const castSlice = createSlice({
    name: 'castSlice',
    initialState,
    reducers: {},
    extraReducers: {

    }
})